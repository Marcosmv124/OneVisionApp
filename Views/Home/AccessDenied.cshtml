@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container text-center mt-5">
    <h1 class="text-danger">
        <i class="bi bi-shield-lock-fill"></i> Acceso Denegado
    </h1>

    @if (User.Identity.IsAuthenticated)
    {
        <p class="lead">
            Estás autenticado como <strong>@User.Identity.Name</strong>, pero no tienes permiso para acceder a esta sección.
        </p>
    }
    else
    {
        <p class="lead">
            No has iniciado sesión o tu sesión ha expirado.
        </p>
    }

    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">
        <i class="bi bi-house-door-fill"></i> Volver al inicio
    </a>
</div>
