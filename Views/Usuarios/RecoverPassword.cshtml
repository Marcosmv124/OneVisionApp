@model One_Vision.DTOs.RecuperarPasswordRequest

@{
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
    ViewData["Title"] = "Recuperar Contraseña";
}
<style>
    .btn-color {
        background-color: #253449;
        color: white;
    }

        .btn-color:hover {
            background-color: #C9B489;
            color: white;
        }
</style>

<div style="text-align: center; margin-top: 50px">
    <img src="~/css/logo.png" alt="logo" style="width: 300px; height: 200px;" />
</div>

<div class="cont-ainer">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0 mt-5">
                <div class="card-body p-4">
                    <h3 class="text-center mb-4">
                        <i class="bi bi-envelope-lock me-2"></i>Recuperar Contraseña
                    </h3>

                    <form asp-controller="Usuarios" asp-action="EnviarCorreoRecuperacion" method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label asp-for="Correo" class="form-label">Correo Electrónico</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                                <input asp-for="Correo" class="form-control" required />
                            </div>
                            <span asp-validation-for="Correo" class="text-danger"></span>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-color">
                                <i class="bi bi-send me-2"></i>Enviar Enlace
                            </button>
                        </div>
                    </form>

                    <div class="text-center mt-3">
                        <a asp-action="Index" asp-controller="Usuarios" class="btn btn-link">← Volver al login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

